# 模型路由测试用例模板（Model Routing Case Template）

> 本模板用于创建模型路由测试用例。
> 多模型路由稳定化

---

## 一、测试用例基本信息

| 字段 | 值 |
|------|-----|
| 用例编号 | `[CASE-XXX]` |
| 用例名称 | `[用例名称]` |
| 测试类型 | `[默认档位/升级/降级/回退]` |
| 创建日期 | `[YYYY-MM-DD]` |
| 创建人 | `[创建人]` |

---

## 二、测试场景描述

### 2.1 场景背景

```
[描述测试场景的背景和目的]
```

### 2.2 预期测试目标

| 目标 | 说明 |
|------|------|
| Skill 路由 | `[预期路由到的 Skill]` |
| 模型档位 | `[预期选择的模型档位]` |
| 输出质量 | `[预期输出质量等级]` |

---

## 三、测试输入

### 3.1 用户输入

```
[用户的原始输入内容]
```

### 3.2 上下文信息

```json
{
  "session_id": "[会话ID]",
  "user_id": "[用户ID]",
  "history": [
    {"role": "user", "content": "[历史消息1]"},
    {"role": "assistant", "content": "[历史回复1]"}
  ],
  "metadata": {
    "[其他元数据]": "[值]"
  }
}
```

### 3.3 环境条件

| 条件 | 值 |
|------|-----|
| 模型可用性 | `[strong/standard/light 可用状态]` |
| 成本预算 | `[当前会话成本预算]` |
| 历史档位 | `[同类任务历史使用的档位]` |

---

## 四、预期输出

### 4.1 路由结果

| 字段 | 预期值 |
|------|--------|
| 目标 Skill | `[Skill 名称]` |
| 选择档位 | `[strong/standard/light]` |
| 选择原因 | `[为什么选择该档位]` |

### 4.2 输出内容

```
[预期的输出内容结构或示例]
```

### 4.3 响应时间

| 档位 | 预期响应时间 |
|------|--------------|
| strong | `< 60秒` |
| standard | `< 30秒` |
| light | `< 15秒` |

---

## 五、模型选择分析

### 5.1 默认档位检查

```
检查项：
- [x] Skill 默认档位：[档位]
- [x] 是否满足默认条件：[是/否]
- [x] 默认档位选择原因：[原因]
```

### 5.2 升级分析

```
升级条件检查：
- [ ] 任务复杂度 >= 高
- [ ] 输出长度 >= 长
- [ ] 一致性要求 >= 高
- [ ] 其他：[条件]

是否需要升级：[是/否]
升级原因：[原因]
```

### 5.3 降级分析

```
降级条件检查：
- [ ] 任务复杂度 <= 低
- [ ] 输出长度 <= 短
- [ ] 质量要求 <= 低
- [ ] 其他：[条件]

是否需要降级：[是/否]
降级原因：[原因]
```

### 5.4 稳定性检查

```
历史使用检查：
- 同类任务历史档位：[档位]
- 是否存在档位波动：[是/否]
- 稳定性约束是否触发：[是/否]
```

---

## 六、回退分析（如适用）

### 6.1 回退触发条件

| 条件 | 是否满足 |
|------|----------|
| 模型不可用 | [是/否] |
| 成本超限 | [是/否] |
| 响应超时 | [是/否] |
| 效果不稳定 | [是/否] |

### 6.2 回退路径

```
原档位：[档位]
    ↓
回退档位：[档位]
    ↓
最终档位：[档位]
```

### 6.3 回退后输出

```
[回退后的预期输出结构]
```

---

## 七、测试执行记录

### 7.1 执行结果

| 字段 | 实际值 | 预期值 | 是否一致 |
|------|--------|--------|----------|
| 目标 Skill | `[实际]` | `[预期]` | [是/否] |
| 选择档位 | `[实际]` | `[预期]` | [是/否] |
| 响应时间 | `[实际]` | `[预期]` | [是/否] |

### 7.2 实际输出

```
[实际输出内容]
```

### 7.3 差异分析

```
[如有差异，分析差异原因]
```

---

## 八、测试结论

### 8.1 测试结果

| 项目 | 结果 |
|------|------|
| 路由正确性 | `[通过/失败]` |
| 档位选择正确性 | `[通过/失败]` |
| 输出质量 | `[符合/不符合预期]` |
| 响应时间 | `[符合/不符合预期]` |

### 8.2 总体结论

```
[通过/失败]

原因：[说明]
```

### 8.3 改进建议

```
[如有问题，提出改进建议]
```

---

## 九、关联用例

| 用例编号 | 关系 | 说明 |
|----------|------|------|
| `[CASE-XXX]` | 前置 | `[说明]` |
| `[CASE-XXX]` | 后置 | `[说明]` |
| `[CASE-XXX]` | 关联 | `[说明]` |

---

## 十、版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | `[YYYY-MM-DD]` | 初始版本 |
